pipeline {
    agent {label 'mac'}

    tools {nodejs "node"}

    stages {
        stage('Dependencies') {
            steps {
                sh 'npm i'
            }
        }

        stage('Cypress test') {
            steps {
                sh 'npm run cy:run_p'
            }
        }
        
    }
}